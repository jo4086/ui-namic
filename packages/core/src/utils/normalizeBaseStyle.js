import buildStyleBlocks from './builds/buildStyleBlocks'

export const normalizeBaseStyle = (baseProps, META) => {
    // console.log('baseProps:', baseProps)

    buildStyleBlocks()
}

export default normalizeBaseStyle
